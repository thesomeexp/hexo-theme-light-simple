<% if (item.tags && item.tags.length){ %>
  <%
  var tags = [];
  item.tags
    .sort((a, b) => a.localeCompare(b))
    .forEach(function(tag){
      if (tag.path && tag.name) {
        tags.push('<a href="' + config.root + tag.path + '">' + tag.name + '</a>');
      } else {
        tags.push('<a href="' + config.root + config.tag_dir + '/' + tag + '">' + tag + '</a>');
      }
    });
  %>
  <div class="tags">
    <%- tags.join(', ') %>
  </div>
<% } %>